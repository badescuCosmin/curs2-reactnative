import { StyleSheet } from "react-native";

const styles = () =>
  StyleSheet.create({
    messageContainer: {
      gap: 5,
      padding: 2,
      margin: 2,
      backgroundColor: "#C4C4C4",
      borderRadius: 5,
      maxWidth: "80%",
    },
  });

export default styles;
